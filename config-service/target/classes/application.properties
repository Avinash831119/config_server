server.port=8888
spring.application.name=config-service

#spring.cloud.config.server.git.uri=/home/analytics/CUNA_Perform/configuration/perform_config
spring.cloud.config.server.git.uri=c:/Users/veravina/source/perform_config


# Eureka service details
#eureka.client.service-url.default-zone=http://10.96.2.130:8761/eureka/
eureka.client.service-url.default-zone=http://localhost:8761/eureka/
#indicates the frequency the client sends heartbeat to server to indicate that it is alive.
eureka.instance.lease-renewal-interval-in-seconds=30
#indicates the duration the server waits since it received the last heartbeat before it can evict an instance from its registry
eureka.instance.lease-expiration-duration-in-seconds=90

# Load balancing
ribbon.eureka.enabled=true